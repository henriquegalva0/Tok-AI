# Comandos para criar o repositório Tok-AI no GitHub

## ❌ ERRO: Repository not found
# Isso acontece porque o repositório ainda não foi criado no GitHub

## 🔧 SOLUÇÃO:

### Passo 1: Criar o repositório no GitHub
1. Abra o navegador e vá para: https://github.com/henriquegalva0
2. Clique no botão verde "New" ou "Novo repositório"
3. Preencha:
   - Repository name: `Tok-AI`
   - Description: `Projeto de jogos interativos em Python com Pygame`
   - Deixe público (Public)
   - **IMPORTANTE**: NÃO marque "Add a README file" (já temos um)
   - **IMPORTANTE**: NÃO marque "Add .gitignore" (já temos um)
4. Clique em "Create repository"

### Passo 2: Depois que criar o repositório, execute no terminal:

```bash
# Verificar se o remote está correto
git remote -v

# Se precisar, remover e adicionar novamente
git remote remove origin
git remote add origin https://github.com/henriquegalva0/Tok-AI.git

# Adicionar todos os arquivos
git add .

# Fazer commit
git commit -m "Initial commit: Tok-AI project with Pygame games"

# Renomear branch para main
git branch -M main

# Fazer push
git push -u origin main
```

### Passo 3: Se der erro de autenticação
```bash
# Opção 1: GitHub CLI
gh auth login

# Opção 2: Configurar credenciais
git config --global user.name "henriquegalva0"
git config --global user.email "seu-email@exemplo.com"
```

### Passo 4: Verificar se funcionou
```bash
git status
git remote -v
```

## ✅ Resultado esperado:
- Repositório criado em: https://github.com/henriquegalva0/Tok-AI
- Todos os arquivos enviados
- README.md visível na página do GitHub 